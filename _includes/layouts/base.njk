<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>les éditions du samedi</title>
    <link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css">
  </head>
  <body>
    <header>
        <img src="/assets/images/logosamedi.png">
        <h1><a href="/">les éditions du samedi</a></h1>
        <nav>
            <ul>
            {% for post in collections.pages %}
            <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
            {% endfor %}
            </ul>
        </nav>
    </header>
    <main>
	    {{ content | safe }}
    </main>
  </body>
  <footer>Site en construction</footer>
</html>